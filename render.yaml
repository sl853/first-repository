services:
  - type: web
    name: deal-radar
    env: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_VERSION
        value: 22
      - key: PGSSLMODE
        value: require
      - key: SEED_SAMPLE_DEALS
        value: "false"
